replicaCount: 1

image:
  repository: 370180090626.dkr.ecr.us-west-2.amazonaws.com/home
  pullPolicy: Always
  tag: final4

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

secret:
  AWS_REGION: dXMtd2VzdC0y
  DB_NAME: 
  DB_ENDPOINT_READER: 
  DB_PORT: 
  DB_ENDPOINT_WRITER: 
  AWS_SECRET_NAME: ""
  TABLE_NAME: dXNlcnM=


serviceAccount:
  create: true       # Whether to create the ServiceAccount
  automount: true    # Whether to automatically mount the ServiceAccount token
  annotations: {}    # Annotations for the ServiceAccount, if any
  name: "application"  # Name of the ServiceAccount
  arn: ""            # ARN of the IAM role associated with the ServiceAccount



podAnnotations: {}
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80
#   annotations: {}

ingress:
  enabled: true
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: "*"
      paths:
        - path: /
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

# resources: 
#   limits:
#     cpu: 100m
#     memory: 128Mi
#   requests:
#     cpu: 100m
#     memory: 128Mi

# livenessProbe:
#   httpGet:
#     path: /
#     port: http
# readinessProbe:
#   httpGet:
#     path: /
#     port: http

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}
